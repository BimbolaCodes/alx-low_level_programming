#include "main.h"
/**
 * main - Entry point
 *
 * Description: This programs prints an alphabet from a-z
 *
 * Return: Return always 0
 */

void print_alphabet(void)
{
	int ch
	for (ch = 'a'; ch <= 'z'; ch++)
	putchar();
	return (0);
}
