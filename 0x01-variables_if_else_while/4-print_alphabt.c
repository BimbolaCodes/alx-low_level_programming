#include <stdio.h>
/**
* main - entry point
* Return: always returns 0
*
*/
int main(void)
{
	int a = 0;

	for = ('a'; a <= 'z' ; a++)
	{
		if (a == 'q' || a == 'e')
		{
			continue;
		}
		putchar(a);
	}
	putchar('\n');
	return (0);
}
